[pytest]
addopts = -m "not integration"
markers =
    integration: Tests requiring external services or large external datasets.
    external: Integration tests requiring large external datasets or repo-specific artifacts.
