# OpenRouter API key for real embedding generation
# Get yours at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Set to "true" to use deterministic mock embeddings (no API calls)
# Default: false (uses real OpenRouter embeddings)
USE_MOCK_EMBEDDING=true

# Git token examples for workspace pipeline
# export this (or repo-specific env vars referenced by manifest repos[].token_env)
GITLAB_TOKEN=glpat-your-token-here
